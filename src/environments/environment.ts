// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  cardToken:"pk_test_AUzflc5ISc55liSVEbCldftE00SdgG1OM0",

  //  API_ENDPOINT: "https://localhost:44398/api/",// demo
  //  APPLICATIONURL:"http://beta.woozzl.com/new2/",

  API_ENDPOINT: "https://api-netcore.azurewebsites.net/ptstage/api/",
  APPLICATIONURL: "http://proptecs.com/Demo/",

  // API_ENDPOINT: " https://api-equityone-prod.azurewebsites.net/api/",// Production
  // APPLICATIONURL:"http://beta.woozzl.com/",


};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
